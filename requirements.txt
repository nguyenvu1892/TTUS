# ============================================================
# TikTok Shop Affiliate Farm — Python Dependencies
# ============================================================
#
# Task 1 (ld_manager.py): stdlib only — sem pacotes externos.
#
# Task 2 (proxy_manager.py):
#   requests>=2.28.0   — Download APK via HTTP(S) com streaming
#                        e suporte a retry. Vastamente superior
#                        a urllib para payloads binários grandes.
#   colorama>=0.4.6    — Cores ANSI no terminal Windows. Sem esta
#                        lib os códigos de escape imprimem como
#                        texto raw no PowerShell/CMD.
#
# Instalação manual (caso o auto-bootstrap falhe):
#   pip install -r requirements.txt
# ============================================================

requests>=2.28.0
colorama>=0.4.6
